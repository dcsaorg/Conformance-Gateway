Then press "Refresh status" to refresh the scenario status and conformance checks.

When done, press "Action completed" to mark this scenario action completed.

Alternatively, press "Skip action" if you choose not to perform this optional GET action. The skipped action will be
shown with "↪️" and will not prevent the scenario from being conformant.
