Perform Use Case 19: `DECISION` the direct amendment for the Transport Document with reference `REFERENCE`.

* Process the latest direct amendment asynchronously.
* Set `amendedTransportDocumentStatus` to `AMENDMENT_CONFIRMED` when confirming, or `AMENDMENT_DECLINED` when declining.
* Keep the original `transportDocumentStatus` unchanged.


