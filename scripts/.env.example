# Environment variables for the conformance scripts.
# Copy this file to .env and fill in the values.
# .env is gitignored — never commit real credentials.

# AWS credentials & region
AWS_REGION=eu-north-1
# AWS_PROFILE=my-profile   # optional: use a named AWS CLI profile instead of keys
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=       # only needed for temporary/assumed-role credentials

# ---------------------------------------------------------------------------
# Pick ONE environment block and uncomment it (or add your own).
# ---------------------------------------------------------------------------

# --- dev ---

# --- dt ---

# --- test ---
COGNITO_USER_POOL_ID=eu-north-1_VRXXJfTsv
DYNAMODB_TABLE_NAME=testConformance

