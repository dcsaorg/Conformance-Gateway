Send a GET request to the sandbox endpoint '/events' with the following mandatory query parameter:

- EVENT_TYPE_PARAM_NAME=EVENT_TYPE_VALUE

If you need, include other additional query parameters in the same request.

The sandbox will respond with events matching your query parameters.
