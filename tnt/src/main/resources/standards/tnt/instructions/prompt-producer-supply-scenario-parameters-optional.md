Provide values that the sandbox can use when it sends GET requests to your T&T Event Producer at `/events`.

The scenario always enforces the target `eventTypes` value. In addition, you must provide at least one of the base
filter parameter values listed below. Each parameter is individually optional, but the request must include one or more.

**Query parameters (at least one required)**
OPTIONAL_PARAMETERS_PLACEHOLDER

Use the example JSON below and remove any parameters that you do not want to supply for this run, keeping at least one
field to include in the request.


