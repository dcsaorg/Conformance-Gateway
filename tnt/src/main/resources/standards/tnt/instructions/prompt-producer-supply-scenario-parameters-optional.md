Provide values that the sandbox can use when it sends GET requests to your T&T Event Producer at `/events`.

The scenario always enforces the target `eventTypes` value. In addition, you may provide any optional base filter
parameter values listed below.

**Optional query parameters**
OPTIONAL_PARAMETERS_PLACEHOLDER

You may remove any optional parameter that you do not want to supply for this run. Use the example JSON below and keep
only the fields that should be included in the request.


