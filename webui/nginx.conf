server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Handle Angular HTML5 routing — always return index.html for unknown paths
    location / {
        try_files $uri $uri/ /index.html;
    }
}

